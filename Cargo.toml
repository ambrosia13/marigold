[workspace]
resolver = "3"
members = ["bvh", "bvh_sample_collector", "gltf_loading", "marigold", "mesh_interface"]

# since these components are performance critical and math heavy,
# depend on them in release mode
[profile.dev.package.bvh]
opt-level = 3
