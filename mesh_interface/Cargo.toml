[package]
name = "mesh_interface"
version = "0.1.0"
edition = "2024"

[dependencies]
vulkano.workspace = true
bytemuck.workspace = true

# game engine stuff
glam.workspace = true

# general utility
derived-deref.workspace = true

# workspace dependencies
bvh = { path = "../bvh" }
