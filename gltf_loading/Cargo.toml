[package]
name = "gltf_loading"
version = "0.1.0"
edition = "2024"

[dependencies]
# game engine stuff
glam = { version = "0.33.3", features = ["bytemuck"] }
gltf = "1.4.1"

# logging
log = "0.4.29"

# workspace dependencies
bvh = { path = "../bvh" }
mesh_interface = { path = "../mesh_interface" }
