[package]
name = "gltf_loading"
version = "0.1.0"
edition = "2024"

[dependencies]
# game engine stuff
glam.workspace = true
gltf.workspace = true

# logging
log.workspace = true

# workspace dependencies
bvh = { path = "../bvh" }
mesh_interface = { path = "../mesh_interface" }
